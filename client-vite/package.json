{
  "name": "vite-mpa",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "mock-server": "json-server --watch db.json --port 3000",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "json-server": "^0.9.6",
    "vite": "^5.0.0"
  }
}
